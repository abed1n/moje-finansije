# Opste postavke
# Port se cita iz PORT promjenljive ako je postavljena (npr. Render), inace 8080
quarkus.http.port=${PORT:8080}
quarkus.http.limits.max-body-size=10M

# JWT autentifikacija
mp.jwt.verify.publickey.location=jwt/publicKey.pem
mp.jwt.verify.issuer=personal-finance-manager
smallrye.jwt.sign.key.location=jwt/privateKey.pem
# Pristupni token je kratkotrajan; refresh token (u httpOnly kolacicu) ga tiho obnavlja
app.jwt.access-token-minutes=15
app.jwt.refresh-token-days=30
# Refresh kolacic uvijek je httpOnly + SameSite=Strict. Secure flag (samo preko HTTPS-a)
# je podrazumijevano iskljucen da lokalni Docker demo radi preko HTTP-a; pravi HTTPS
# deploy ga ukljucuje sa COOKIE_SECURE=true.
app.auth.cookie-secure=false
%prod.app.auth.cookie-secure=${COOKIE_SECURE:false}

# Direktorij za priloge transakcija
app.uploads.dir=uploads

# Osnovni URL aplikacije - koristi se za linkove u email porukama
app.base-url=http://localhost:8080

# Email (reset lozinke, potvrda adrese). U dev/test/demo mailer je mock:
# poruke se ne salju nego ispisuju u log. Pravi SMTP se ukljucuje u prod-u kroz env.
quarkus.mailer.from=Moje finansije <no-reply@moje-finansije.me>
%dev.quarkus.mailer.mock=true
%test.quarkus.mailer.mock=true
%prod.quarkus.mailer.mock=${MAILER_MOCK:true}
%prod.quarkus.mailer.host=${MAILER_HOST:smtp.example.com}
%prod.quarkus.mailer.port=${MAILER_PORT:587}
%prod.quarkus.mailer.username=${MAILER_USERNAME:}
%prod.quarkus.mailer.password=${MAILER_PASSWORD:}
%prod.quarkus.mailer.start-tls=${MAILER_TLS:REQUIRED}
%prod.quarkus.mailer.from=${MAILER_FROM:Moje finansije <no-reply@moje-finansije.me>}
%prod.app.base-url=${APP_BASE_URL:http://localhost:8080}

# Ogranicenje prijava: nakon max-attempts neuspjelih pokusaja blokada na lockout-minutes
app.login.max-attempts=5
app.login.lockout-minutes=15

# Koliko sati vazi link za potvrdu email adrese
app.email-verification.token-hours=24
# Da li je potvrda emaila obavezna prije prijave. Iskljuci (false) na hostingu
# koji ne dozvoljava slanje emaila - tada je nalog odmah aktivan po registraciji.
app.email-verification.required=${EMAIL_VERIFICATION_REQUIRED:true}

# OpenAPI / Swagger UI (dostupno na /q/swagger-ui)
quarkus.smallrye-openapi.info-title=Personal Finance Manager API
quarkus.smallrye-openapi.info-version=1.0
quarkus.swagger-ui.always-include=true

# REST klijenti (vanjski API-ji)
quarkus.rest-client.ip-api.url=https://api.ipify.org
quarkus.rest-client.timezone-api.url=https://timeapi.io
quarkus.rest-client.location-api.url=https://ipapi.co
quarkus.rest-client.currency-api.url=https://api.frankfurter.dev
quarkus.rest-client.google-oauth.url=https://oauth2.googleapis.com

# Google prijava: client id iz Google Cloud konzole. Ako je prazan, opcija je
# iskljucena (dugme se ne prikazuje). Postavlja se kroz GOOGLE_CLIENT_ID.
google.client-id=${GOOGLE_CLIENT_ID:}

# Dev profil (lokalni PostgreSQL)
%dev.quarkus.datasource.db-kind=PostgreSQL
%dev.quarkus.datasource.username=pfm_user
%dev.quarkus.datasource.password=admin123
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/pfm_db
%dev.quarkus.hibernate-orm.schema-management.strategy=drop-and-create
%dev.quarkus.hibernate-orm.log.sql=true
%dev.app.seed-demo-data=true

# Test profil (H2 u memoriji, ne zahtijeva Docker ni PostgreSQL)
%test.quarkus.datasource.db-kind=H2
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:pfm;DB_CLOSE_DELAY=-1
%test.quarkus.hibernate-orm.schema-management.strategy=drop-and-create
%test.app.seed-demo-data=false

# Prod profil (sve vrijednosti dolaze iz environment varijabli;
# DB_PASSWORD je obavezan - aplikacija se bez njega ne pokrece)
%prod.quarkus.datasource.db-kind=PostgreSQL
%prod.quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/pfm_db}
%prod.quarkus.datasource.username=${DB_USER:pfm_user}
%prod.quarkus.datasource.password=${DB_PASSWORD}
%prod.quarkus.datasource.jdbc.max-size=16
# Serverless baze (npr. Neon) "zaspu" kad su neaktivne pa prvo povezivanje
# treba par sekundi da ih probudi - dajemo mu dovoljno vremena umjesto 5s.
%prod.quarkus.datasource.jdbc.acquisition-timeout=30S
%prod.quarkus.datasource.jdbc.min-size=0
%prod.quarkus.datasource.jdbc.initial-size=0
%prod.quarkus.hibernate-orm.log.sql=false
%prod.app.seed-demo-data=${SEED_DEMO_DATA:false}
%prod.app.uploads.dir=${UPLOADS_DIR:uploads}

# Sema se u produkciji kreira iskljucivo kroz Flyway migracije (db/migration),
# Hibernate je samo validira. U dev/test profilu ostaje drop-and-create.
%prod.quarkus.flyway.migrate-at-start=true
%prod.quarkus.hibernate-orm.schema-management.strategy=validate
%dev.quarkus.flyway.migrate-at-start=false
%test.quarkus.flyway.migrate-at-start=false

# JWT kljucevi se u produkciji mogu zamijeniti vlastitim bez rebuilda
# (mount + putanje kroz environment varijable)
%prod.mp.jwt.verify.publickey.location=${JWT_PUBLIC_KEY_LOCATION:jwt/publicKey.pem}
%prod.smallrye.jwt.sign.key.location=${JWT_PRIVATE_KEY_LOCATION:jwt/privateKey.pem}
